import javax.swing.JPanel;

interface SubPanel {  //no need for this interface
	JPanel panel = new JPanel();//diagram needs to change, private isn't
	//allowed in interfaces, we have them in diagram -G
}
